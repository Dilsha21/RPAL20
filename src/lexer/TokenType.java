package lexer;

// TokenType.java
// Defines token categories produced by the RPAL Lexer: IDENTIFIER, INTEGER, STRING, KEYWORD, OPERATOR, PUNCTUATION, EOF.
public enum TokenType {

    // Literals 
    IDENTIFIER,
    INTEGER,
    STRING,
    KEYWORD,
    OPERATOR,
    PUNCTUATION,
    EOF
}